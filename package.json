{
  "name": "recipe-generator",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "axios": "^1.7.2",
    "@vercel/node": "^4.0.0",
    "lru-cache": "^10.2.0"
  },
  "overrides": {
    "esbuild": ">=0.24.3",
    "path-to-regexp": ">=6.2.3"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "mkdir -p public && cp -r api public/",
    "start": "vercel dev"
  }
}
