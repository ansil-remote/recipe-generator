{
  "builds": [
    {
      "src": "api/*.js",
      "use": "@vercel/node",
      "config": { 
        "includeFiles": ["api/**"],
        "supportsResponseStreaming": true
      }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/generate.js" },
    { "src": "/(.*)", "dest": "public/index.html" }
  ]
}